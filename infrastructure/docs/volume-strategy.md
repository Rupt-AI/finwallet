# FinWallet Volume Strategy

Volumes:

- finwallet-kafka-data
- finwallet-postgres-data
- finwallet-redis-data

Purpose:

Ensure persistence across container restarts.

Containers may be destroyed without data loss.